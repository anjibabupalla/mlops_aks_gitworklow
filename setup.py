from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='migrate existing ml to azure devops and deploy to aks',
    author='abpalla',
    license='MIT',
)
